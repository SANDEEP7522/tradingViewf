// Phase 2-5 me replace hoga — abhi routes wire karne ke liye
const Placeholder = ({ title }: { title: string }) => (
  <section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-10">
    <h1 className="text-2xl font-semibold">{title}</h1>
    <p className="mt-2 text-slate-400">Ye page abhi banana baaki hai (coming soon).</p>
  </section>
)

export default Placeholder
